# MCMC array to data frame

MCMC array to data frame

## Usage

``` r
.array2dfr(object, value_to = "value")
```

## Arguments

- object:

  `bdm` class object

- value_to:

  column header for array value
